# Known tokio::select! branches that consume a message they can lose (#926, tracked in #933).
#
# EMPTY — all 21 instances were fixed (#932 translator, #934 pool-sv2, #936 jd-client/jd-server).
# The check now fails on ANY occurrence, so this file staying empty is the invariant.
#
# One line per instance, "path handler_name", if debt is ever reintroduced deliberately. Keyed by
# handler rather than line number so an entry survives edits above it.
#
# these are not equivalent in severity. A loop whose select! has TWO OR MORE message
# branches loses continuously under load, because a sibling is ready on nearly every poll. A loop
# with one message branch plus a cancellation token can only lose on shutdown.
