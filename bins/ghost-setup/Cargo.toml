[package]
name = "ghost-setup"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Headless CLI for Ghost Pool node setup"

[[bin]]
name = "ghost-setup"
path = "src/main.rs"

[dependencies]
ghost-common = { path = "../../crates/ghost-common" }
clap = { workspace = true }
dirs = "5.0"
